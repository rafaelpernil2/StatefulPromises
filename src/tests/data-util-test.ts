import { expect } from 'chai';
import ko from 'knockout';
import 'mocha';
import { PromiseBatch } from '../index';
import { PromiseBatchStatus } from '../index';
import { IAnyObject } from '../interfaces/i-any-object';
import { ICustomPromise } from '../interfaces/i-custom-promise';
import { PromiseUtil } from '../utils/promise-util';

describe('DataUtil.isPromiseBatchCompleted()', () => {
  it('does something', async () => {
    // Implement if needed
  });
});

describe('DataUtil.isPromiseBatchFulfilled()', () => {
  it('does something', async () => {
    // Implement if needed
  });
});

describe('DataUtil.buildStatefulPromise()', () => {
  it('does something', async () => {
    // Implement if needed
  });
});
